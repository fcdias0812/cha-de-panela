# scripts/

Esta pasta guarda scripts de apoio do projeto (ex: `deploy`, `backup`).

No molde, ela nasce vazia de propósito — a skill `deploy` (feita pelo TI) é quem
coloca aqui o script de publicação com backup do banco e rollback automático,
conforme o `GUIA_TECNICO_TI.md` (seção 7).
